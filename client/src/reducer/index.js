const initialState = {
    freeHours: []
}

function reducer(state = initialState, { type, payload }) {
    switch (type) {
        default:
            return {
                state
            }
    }
}

export default reducer;